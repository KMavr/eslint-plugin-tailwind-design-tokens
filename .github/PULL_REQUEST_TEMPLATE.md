## Summary

<!-- What does this change and why? -->

## Type of change

<!-- Pick the one matching your commit type (drives the release). -->

- [ ] `fix:` — bug fix (patch)
- [ ] `feat:` — new feature (minor)
- [ ] `docs:` / `chore:` / `test:` / `ci:` / `refactor:` — no release

## Checklist

- [ ] Tests added/updated and `npm test -- --run` passes
- [ ] `npm run lint` and `npm run typecheck` pass
- [ ] If a rule's `meta` changed, ran `npm run doc` to regenerate the docs
- [ ] Commit messages follow Conventional Commits
- [ ] Updated relevant docs (`docs/rules/*.md` prose, README, `examples/`) if behaviour changed
