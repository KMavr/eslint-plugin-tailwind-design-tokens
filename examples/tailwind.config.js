/* Tailwind v3 design tokens — read by no-hardcoded-colors via the `configFile` option. */
module.exports = {
  theme: {
    extend: {
      colors: {
        surface: '#f5f5f5',
      },
    },
  },
};
