import type { ESLint } from 'eslint';

const plugin: ESLint.Plugin = {
  meta: {
    name: 'eslint-plugin-tailwind-design-tokens',
    version: '0.0.0',
  },
  rules: {},
  configs: {},
};

export = plugin;
